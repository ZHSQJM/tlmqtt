- 消息载荷格式治时期
  - 0 表示二进制
  - 1 表示utf-8 但实际上这个值在各个主流的broker都没有做限制 这里制作解析
- 消息过期时间
  - 保留消息存在过期时间时，当超过这个过期时间后再有新的客户端订阅了该主题，不发生保留消息
  - 遗嘱消息的过期时间，当遗嘱消息存在过期时间时，
  - 会话保留时候的qos1与qos2的消息存在过期时间，超过过期时间后的消息不发生，没超过的消息的过期时间设置为还剩下多少时间
  - 如果会话过期了，那么qos1与qos2的消息就不会发生了。尽管消息没有过期，但也不发了
- contentType
  - 表示消息的内容 目前没什么用
- subscriptionIdentifier(订阅标识符)
  - 用于客户端快速找到收到的消息是属于哪个主题的
  - 一般来说如果客户端收到可服务端的消息时，消息会带有主题的名称，这时候客户端如果想要知道是哪个主题发送的消息然后执行对应的方法就需要一个个的匹配，但是如果在订阅的实际就在使用了这个订阅标识符，那么服务器在
  之后转发这个主题的消息时候，就把这个订阅标识符带上，然后转发给客户端，客户端在订阅的时候保存订阅标识符与主题的映射表，然后就能快速找到这个关系，执行对应的方法。不同的主题可以设置同样的订阅标识符
- 请求与响应
  - 由于mqtt是异步通信的。也就是说，如果A客户端发送了一个消息给B客户端。此时A客户端并不知道B客户端是否成功收到该消息。在mqtt5.0之前的做法是。在A客户端订阅一个主题。然后B客户端收到消息后向
A客户端订阅的主题中发送接收成功的消息。但是这会存在2个问题。第一个问题是这个A客户端接收响应的主题需要提前设置，也就是如果多个客户端都需要这样做的话就需要设置多个，第二个问题是如果使用同一个，如果使用了同一个
，那么也不清楚到底是哪个客户端响应的。所以mqtt5.0引入了请求与响应，在发送消息的时候可以指定响应的主题，并且可以附加一个data作为标识，那么响应的收到该消息的客户端在回复消息的时候也将该消息的标识带回去，这样既可以
识别是哪个客户端的回复的
